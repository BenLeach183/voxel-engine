#pragma once
#include "World/BlockTexture.h"
	
	/*
		Stores information about
		blocks based on the
		block id's.

		Stores texture id's for
		each face.
	*/

class BlocksList {
public:
	// array of block info
	static BlockTexture blocksInfoList[];
};